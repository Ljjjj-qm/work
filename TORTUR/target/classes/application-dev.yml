server:
  port: 8080

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/tutor?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=UTC
    username: root
    password: andafw42

# 日志配置
logging:
  level:
    root: info
    com.rjxy: debug
  file:
    name: log/blog-dev.log
